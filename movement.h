void makeAMove(struct Board* board); //asks player for a coordinate to move a chosen penguin
void choosePenguin(struct Board* board); //chooses penguin for movement
int checkMove(struct Board* board, struct Player* current); //checks if there are any possible moves left for a penguin